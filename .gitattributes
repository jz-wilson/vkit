# Hooks and shell scripts MUST be LF — a CRLF `#!/usr/bin/env bash` script
# fails on Linux/macOS/WSL with "bad interpreter: ...^M".
*.sh                              text eol=lf
.githooks/**                      text eol=lf
internal/scaffold/templates/.githooks/** text eol=lf
